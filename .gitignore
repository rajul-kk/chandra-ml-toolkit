__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# downloaded/cached data - reproducible via scripts/fetch_data.py
data/raw/
data/cache/
*.fits

# outputs - results/ is tracked (it's the actual finding), scratch plots aren't
*.png
!results/*.png
!docs/**/*.png

# vendored external repo (AnomalyMatch, MIT licensed, own git history) -
# cloned locally for image_anomaly_detection/, not tracked as part of this repo
image_anomaly_detection/vendor/
image_anomaly_detection/benchmark_results/
